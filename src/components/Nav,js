import React from 'react';

const Nav = ({ step }) => {
  return (
    <div>
      <button disabled={step === 1}>Step 1</button>
      <button disabled={step !== 2}>Step 2</button>
      <button disabled={step !== 3}>Step 3</button>
    </div>
  );
};

export default Nav;
